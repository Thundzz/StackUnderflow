<h1>Page d'accueil des Questions</h1>
<p class="h3">Cette page liste les questions posées</p>
<p><%= link_to "Nouvelle question", new_question_path , :class=>"btn btn-info"%></p>
<div class="question">
  
  <hr/>
  
  <% @questions.each do |question|%>
  <div class="panel panel-default">
    <div class="panel-heading">
      
      <h2 class="panel-title"> <u><%=  link_to question.title, question_path(question) %></u> par <u><%= link_to question.user.login, question.user %></u> </h2>
    </div>
    <div class="panel-body" style="height:30px" >
      <% unless question.tags.blank? %>
        <ul class="tags"> 
        	<% question.tags.each do |tag|%>
        	<li><a href="<%= tag_path(tag)%>"><%= tag.name%></a></li>
        	<%end%>
        </ul>
      <%end%>
      
    </div>
  </div>
  <br/>
  <%end %>

  
  
</div>




<div id="stackoverflow"  >
  <div class="navbar-form">
    <div class="form-group">
      <input id="question_mark" onKeyPress="search_on_stackoverflow();" class="form-control" placeholder="Rechercher sur StackOverflow" type="text"  />
      <input type="button" class="btn btn-default" onClick="search_on_stackoverflow();" value="Ok"/>
    </div>
  </div>
  
  <div id="question_list">
   <!-- Liste des questions sur StackOverflow -->
  </div>
</div>
